# design/refused/

Proposals that were **never approved** — declined at the proposal gate
(`column: refused`). Kept as the audit trail (never deleted). Re-attempting a
refused idea means authoring a NEW proposal that may cite the refused one. See
`~/.claude/rules/trdd-approval-tiers.md`.

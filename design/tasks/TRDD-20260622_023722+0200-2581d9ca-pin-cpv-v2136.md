---
trdd-id: 2581d9ca-2be5-472c-8665-6d0e4b389933
title: Pin CPV to v2.136.1 at every callsite (v2.137.0 REPO LINT hangs CI)
column: complete
created: 2026-06-22T02:37:22+0200
updated: 2026-08-08T08:12:50+0200
superseded-by: 4KA9L26G
current-owner: integrator
assignee: integrator
priority: 1
severity: HIGH
effort: S
labels: [ci, cpv, pipeline]
task-type: bugfix
parent-trdd: null
relevant-rules: []
release-via: publish
delivery: direct-push
target-branch: main
test-requirements: [lint, typecheck]
audit-requirements: []
review-requirements: []
impacts: [ci-pipeline]
external-refs: ["github.com/Emasoft/claude-plugins-validation/issues/148"]
---

# TRDD-2581d9ca — Pin CPV to v2.136.1 at every callsite

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-22

- **Done + SHIPPED (v1.3.7, CI GREEN):** all 7 CPV callsites pinned to
  `@v2.136.1`. v1.3.7 CI fully green — `Validate` = 75 s (was a 30-min hang),
  `Plugin Validation` = 1.9 min, Lint / Test (ubuntu+macos) / Release / Notify
  all ✓. Commit `5140a72`.
- **NEXT ACTION:** none — task complete. Bump the pin only when CPV ships a fix
  for #148, verified green on CI first.
- **Load-bearing facts:** v2.136.1 REPO LINT = 18 s (green); v2.137.0 master
  HEAD `3ba056d5` REPO LINT = 29-min hang on GitHub runners (2/2 jobs). The
  `uvx` build itself is ~1 s in both — the hang is inside REPO LINT, not the
  build. v2.136.1 tag = `a268114f`; verified-green master commit = `a0a00a12`.
- **Regression reported:** Emasoft/claude-plugins-validation#148.

## Problem

The integrator's CI/release/publish gates fetch CPV with `uvx --from
git+https://github.com/Emasoft/claude-plugins-validation` (unpinned → resolves
to `master` HEAD). CPV released v2.137.0; its `[REPO LINT] (15 languages)`
phase regressed from ~18 s to a ~30-min hang on GitHub-hosted runners. Both
the `CI / Validate` job and the standalone `Plugin Validation` workflow timed
out (2/2) on v1.3.6 (commit `6eb9445`), while v1.3.5 (commit `6378fd1`) on
v2.136.1 was green in 74 s.

This is the downstream effect of the now-closed #139 (canon's broken `@main`
pin): the integrator had fallen back to a bare URL, which tracks LATEST and
therefore inherited the v2.137.0 regression.

## Decision

Pin every CPV callsite to the **`@v2.136.1` tag** — the last-known-good
release, verified green. This is canon's actual *intent* (version-pin for
stability), implemented with a real, working ref instead of the broken
`@main` or fragile bare master. A regressed/stricter future CPV release can no
longer break these gates with no plugin change; the pin is bumped deliberately
once CPV ships a fix for #148.

This composes with the integrator's existing local deviations (gitleaks
removed, no `CLAUDE_PRIVATE_USERNAMES`) which are version-independent, so
pinning to the pre-fix v2.136.1 costs nothing.

## Changes

- `scripts/publish.py` — 4 callsites (`cpv-setup-branch-rules`, `run_gate`,
  `stage_validate` ×2 incl. docstring) → `@v2.136.1`, with a WHY comment.
- `.github/workflows/ci.yml` — `Validate` step URL + comment.
- `.github/workflows/release.yml` — release-gate URL + comment.
- `.github/workflows/validate.yml` — URL + comment; also corrected the prior
  comment that misattributed the 30-min hang to the integrity fetch (the real
  cause is REPO LINT; the pin fixes it, the integrity-skip is only a speedup).

## Verification

- Local `uvx --from git+…@v2.136.1 cpv-remote-validate plugin . --strict`:
  EXIT=0, 22 s, 0/0/0/0, 68 WARNING.
- actionlint (3 workflows), yamllint, `ruff check`, `mypy --ignore-missing-imports`:
  all clean.
- CI verification (pending): v1.3.7 run must show `Validate` + `Plugin
  Validation` green and fast.

## Durable artifacts

- CPV regression report: Emasoft/claude-plugins-validation#148.
- Project memory: `integrator-cpv-canon-deviations` (to be updated — the
  bare-URL note is superseded by this pin).

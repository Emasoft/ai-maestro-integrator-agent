# design/proposals/

TRDDs with `column: proposal` — **authored, awaiting approval, NOT yet
authorized to execute** (Tier 1/2/3 work). On approval the approver sets
`column: planned`, records the decision in the TRDD's `## Approval log`, and
`git mv`s the file into `design/tasks/`. On refusal it moves to
`design/refused/`. See `~/.claude/rules/trdd-approval-tiers.md`.
